# Scenes

Create `Main.scene` in Cocos Creator 3.8, add a Canvas, then attach `assets/scripts/BugSprintGame.ts` to a child node.

The scene file is intentionally left for the editor to generate so Creator can assign script UUID metadata correctly.

