# Assets

This implementation uses runtime drawing only, so no image or atlas assets are required yet.

If you replace the generated shapes with art assets later, place those files here so LayaAir IDE can include them in web exports.

